## 2026-08-15

- `nf -m "메시지"`가 숨김 PowerShell 프로세스 생성만으로 성공 처리하지 않도록 고치고, Unicode Windows 확인 팝업을 15초 동안 표시하며 실제 전달 실패를 nonzero exit로 반환한다.

## 2026-08-16

- `nf` 확인 팝업의 제한시간을 제거하고, 사용자가 확인 버튼을 누른 경우에만 성공으로 처리해 호출 작업이 자동으로 계속되게 한다.
- `nf` 팝업에 30초 제한시간을 다시 두고, 그 안에 확인 버튼을 누르면 즉시 성공하며 응답이 없으면 자동 yes로 처리해 작업을 중단하지 않고 계속 진행한다.
- Fish에 `nf_auto` 확장을 추가한다. Plan Mode에서 완성된 계획의 선택지를 표시하기 직전에 이를 호출하고, 알림 확인 또는 30초 timeout 뒤 5초가 지나면 호출 당시 tmux pane에 Enter를 한 번 입력한다.

## 2026-08-17

- tmux copy-mode에서 `a`를 누르면 선택 경로로 Nvim을 실행하는 `Config/data/tmux/tmux.conf`의 바인딩을 영구 삭제한다.
